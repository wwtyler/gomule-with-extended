# GoMule × D2R 1.5+ — 物品位流尾部字段 (必读)

> ⚠️ **必读 / MUST-READ**
> 任何处理 `.d2s` / `.d2i` / `.d2x` 文件位流解析的工作（GoMule 修改、新写
> 解析器、调试 Huffman 错位、调试 `NumberFormatException` on
> `properties.txt` / `ItemStatCost.txt` 等），开工前必须先读完本文档。
>
> 关键字: D2S parsing, D2R 1.5, item bit stream, Huffman, item alignment,
> `has_quantity`, `has_advanced_stash_quantity`, `has_chronicle_data`.

姊妹文档：[`d2r-d2s-key-notes.md`](./d2r-d2s-key-notes.md) — D2R `.d2s` 整体格式说明。

---

## 1. 症状

### 1.1 Huffman 乱码（外部对齐错位）
```
java.lang.Exception: Unknown item code 'sf4ic94p'
                     (not found in misc/armor/weapons.txt)
```
表现：第 1 件物品解析正常，第 2 件物品位置一偏，霍夫曼字典把字节流读成
8+ 字符的乱码 type code。文件版本为 **99（0x63）或 105（0x69）**。

### 1.2 属性解析空字段（内部对齐错位）
```
java.lang.NumberFormatException: For input string: ""
    at gomule.item.D2PropCollection.readProp(D2PropCollection.java:367)
```
表现：解析到某件 non-simple 物品时，9-bit `rootProp` 落到一个 `Save Bits`
单元格为空的 stat（如 `armorclass`），`Integer.parseInt("")` 抛异常。
通常发生在小护符 / 戒指 / 项链 / 不可堆叠武器或护甲。

---

## 2. 根因

D2R 1.5+（item-format marker = `0x69`）相比经典 D2，每件物品**多出两处比
特位**，旧版 GoMule 不读，导致后续 1+ bit 错位级联：

### 2.1 物品中段（在属性列表之前、socket 计数之后）
对**每一件 non-simple 物品**：
```
has_quantity                    : 1 bit   (恒读)
quantity                        : 9 bit   (仅 has_quantity == 1)
```
旧 GoMule 只在 `iItemType.stackable == "1"` 时才读 9 bit，对非堆叠物品
完全漏读 → 1 bit 错位。

### 2.2 物品末尾（在字节对齐之前）
对每件物品（含 simple）：
```
[if has_chronicle_data (D2 bit 28 = check_flag(29))]
    chronicle_source            : 16 bit
    chronicle_timestamp         : 32 bit
    chronicle_suffix            : 4 bit
has_advanced_stash_quantity     : 1 bit   (恒读)
advanced_stash_quantity         : 8 bit   (仅 has_advanced_stash_quantity == 1)
align to byte boundary
```
旧 GoMule 直接对齐 → 简单药水 `yps` 应消耗 73 bit 对齐到 80 bit (10 字
节)，实际只消耗了 72 bit (已对齐) → 下一件物品起始位置早 1 字节。

---

## 3. 修复位置（本仓库已修）

| 文件 | 修改 |
|------|------|
| [`src/main/java/gomule/item/D2Item.java`](src/main/java/gomule/item/D2Item.java) | 新增静态字段 `public static int sSaveVersion`；构造函数 `read_item()` 之后、`getNextByteBoundaryInBits()` 之前补读 §2.2 尾部位 |
| 同上 `readExtend2()` | socket 计数之前插入 §2.1 中段 `has_quantity` 处理；保留 LoD（v < 0x69）原行为 |
| [`src/main/java/gomule/d2s/D2Character.java`](src/main/java/gomule/d2s/D2Character.java) | `readChar()` 校验版本后设置 `D2Item.sSaveVersion = 0x69` |
| [`src/main/java/gomule/d2i/D2SharedStashReader.java`](src/main/java/gomule/d2i/D2SharedStashReader.java) | `readSharedStashPane()` 校验版本后同样设置 |
| [`src/main/java/gomule/d2x/D2Stash.java`](src/main/java/gomule/d2x/D2Stash.java) | `readAtmaItems()` — `lVersionNr == 99` 分支内加 `D2Item.sSaveVersion = 0x69`，已验证 .d2x 正常解析 ✅ |

---

## 4. 参考实现（外部权威源）

D2RMM 内嵌的 TypeScript D2R 解析器（最完整、社区参照标准）：
- `d:\260305\d2rmm\src\main\worker\third-party\d2s\d2\items.ts` 第 600-685 行
  - 第 620-630 行：§2.1 中段 `has_quantity` 处理
  - 第 669-683 行：§2.2 末尾 chronicle + advanced_stash_quantity + Align()

Python 复刻参考解析器（用于离线对比 bit 位置）：
- `d:\260305\d2rmm\tools\analyze_d2s.py` `parse_item()`
  - 第 297-302 行：`has_quantity`
  - 第 322-330 行：chronicle + adv-stash + `reader.align()`

---

## 5. 调试模式

任何位流错位 bug 排查请走以下流程：

1. **取 ground truth** —— 用 Python 解析器导出每件物品的 `bit_start` /
   `consumed`：
   ```powershell
   python d:\260305\d2rmm\tools\trace_items.py
   ```
2. **加 GoMule 端日志** —— 在 `D2Character.readItems()` 循环里临时打印
   `iReader.get_byte_pos()` + `getBitPosition()`。
3. **对齐两边的 byte / bit_off_in_byte** —— 第一件出现错位的物品就是漏
   读字段所在；查 `items.ts` 同位置代码补齐。
4. **完成后务必移除调试 println**，并用 `gradlew createDistribution -x
   test --quiet` 重建 jar 验证。

---

## 6. 已验证存档

```
C:\Users\wangwei\Saved Games\Diablo II Resurrected\mods\D2RMMMDKV3\sor.d2s
  level=91 items=12  ✅ 12 件物品全部解析成功
```
（MDK V3 mod 法师角色，含 yps / ob1×2 / lbt / box / sst×4 / key / amu×2）
