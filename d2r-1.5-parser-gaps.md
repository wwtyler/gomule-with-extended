# GoMule vs d2rmm `.d2s` 解析器逐字段比对（D2R 1.5+, file v=0x69）

本文档为 GoMule `D2Item.java` 与 d2rmm 上游解析器（TypeScript `items.ts` + Python `analyze_d2s.py`）的逐字段差异审计结果，用于追踪 D2R 1.5+ 存档格式适配进度。

## 参考实现（authoritative）

- TypeScript: `d:\260305\d2rmm\src\main\worker\third-party\d2s\d2\items.ts`（`readItem` L347+）
- Python: `d:\260305\d2rmm\tools\analyze_d2s.py`（`parse_item` L150-340）

## 目标文件

- `src/main/java/gomule/item/D2Item.java`
- `src/main/java/gomule/item/D2PropCollection.java`
- `src/main/java/gomule/d2s/D2Character.java`

---

## Gap 总表

| # | 严重度 | items.ts/Python 行为 | GoMule（修前） | 状态 |
|---|---|---|---|---|
| 1 | 🔴 致命 | `timestamp = ReadUInt8(1)`（仅 1 位，无分支） | `lHasGUID = read(1)`，若==1 则按 itype 读 128 或 3 位（经典 LoD GUID 字段） | ✅ **已修** — gate 在 `sSaveVersion < 0x69`（D2Item.java L378） |
| 2 | 🟠 中 | `tome_extra_5`（5 位，紧跟 personalized 之后，仅 tbk/ibk） | 仅在 `readTypes`（quality==Normal 路径）里读 5 位 | ⚪ 实际不触发 — vanilla tbk/ibk 是 simple_item，不进 readExtend1 |
| 3 | 🟠 中 | personalized loop **16** 次 × 8 位 | loop **15** 次 + 若未遇 0 再补 1 字节 | ⚪ **数学等价** — 重新验证后确认两种循环对所有合法名字长度（0-15 字符 + null）行为一致 |
| 4 | � 致命（实测） | Quality.Normal：投机读 12 位，若**全 0/全 1 则保留**（pad 标记），否则回退 -12 | switch case 2 直接调 `readTypes`，**完全没有这个投机读** | ✅ **已修** — D2Item.java case 2 内最前端补上"读 12 位 → 非全 0/全 1 时回退"逻辑（与 items.ts 一致）。**重要修正**：之前误判此 gap 为"低优先级/数学等价"，但 MDK V3 的 Warlock.d2s 实测中，#7 lxb 或后续 Normal 物品的 pad bits 命中"全 0/全 1"，items.ts 保留这 12 位，GoMule 不读 → 后续每个字段都偏前 12 位 → #9 tbk 的 huffman 解出空字符串、magic_attributes 起始位读到 stat_id=4 (statpts) 等字符块数据。 |
| 5 | 🟡 低 | Quest itype：读 `quest_difficulty`（stat 356 SaveBits=2）+ 1 位 sockets | 永远读 3 位 sockets | ⚪ 总位数巧合相等（2+1=3），仅数据值错位 |
| 6 | 🟡 低 | armor/dur 位宽来自 `magical_properties[31/72/73].sB` | 硬编码 11/8/9 | ⚠️ 与 vanilla itemstatcost 一致；MDK V3 实测未改这三行；mod 改了才会错 |
| 7 | ⚪ 多余 | tsc/isc 不读 5 位、body 不读 10 位 | `readTypes` 里都读了 | ⚪ 实际不触发 — 这些 itype 是 simple_item |

## 已修复（前序会话）

| 字段 | 位置 |
|---|---|
| `has_quantity`（1+9）每个 v≥0x69 非 simple 物品 | `D2Item.java` `readExtend2` ~L862 |
| chronicle 尾部（16+32+4，has_chronicle 时） | `readExtend` 尾部 |
| `has_advanced_stash`（1+8）尾部 | `readExtend` 尾部 |
| `parseShortSafe`（itemstatcost 空列降级） | 各处 weapons/armor 取值 |
| `D2PropCollection` 空 SaveBits 抛错 + snapshot | `D2PropCollection.java` `readProp` L367 |
| 日志系统 D2Log/D2LogContext + bit-stream snapshot | `gomule.util.D2Log/D2LogContext`, `D2BitReader.snapshot` |

## 真值参考：itemstatcost.txt（d2111，等同 MDK V3）

| stat ID | name | SaveBits | SaveAdd |
|---|---|---|---|
| 31 | armorclass | 11 | 10 |
| 72 | durability（current） | 9 | 0 |
| 73 | maxdurability | 8 | 0 |
| 356 | questitemdifficulty | 2 | 0 |

## sor.d2s 诊断案例（Sorceress lv91, MDK V3, 12 物品）

参考解析器 `analyze_d2s.py` 输出：

```
#1  yps  Simple
#2  ob1  Magic   (4 props)
#3  ob1  Rare    (7 props)
#4  lbt  Rare    (7 props)
#5  box  Normal  (0 props)   ← Horadric Cube，关键触发点
#6  sst  Normal  (1 props)   ← GoMule 修前在此读到 type='p  m' 乱码
#7-10  各类 sst/key/amu
#11 amu  Rare    PropError: stat_id=388 > max_id=387（itemstatcost.txt 不匹配，非解析器问题）
```

**根因（Gap #1）**：item #5 `box` 是唯一在 #6 之前的"非 Magic/Rare、非 armor/weapon、非 simple"物品，没有 magic_attributes 列表，无法靠 0x1FF 终止符自对齐。其 timestamp 位被旧版 GUID 分支误吞，多读 3 位，错位直接传给 #6 huffman 解码 → `'p  m'`。

修复后预期：12 个物品全部正常解析；#11 的 stat_id=388 仍会触发 `D2PropCollection.readProp` 的 RuntimeException —— 那是 MDK V3 itemstatcost 行数与运行时加载到的版本不一致的问题，需要在 prop collection 里降级为 warn 才能继续。

## 构建命令

```powershell
$env:JAVA_HOME="D:\Code\jdk-21.0.9"
$env:PATH="D:\Code\jdk-21.0.9\bin;"+$env:PATH
cd d:\260305\gomule-with-extended-stash-size\GoMule
.\gradlew.bat createDistribution -x test --quiet
```

启动可执行 jar：

```powershell
cd d:\260305\gomule-with-extended-stash-size\GoMule\build\tmp\distribution\GoMule
& 'D:\Code\jdk-21.0.9\bin\java.exe' -cp GoMule.jar gomule.GoMule
```

## 后续回归思路

1. 改 `D2PropCollection.readProp`，把"未知 stat_id"从 `throw` 降为 `D2Log.warn` + `iProps.add(unknown stat marker)`，避免单个属性错破坏整个物品列表。
2. 收集多份 D2R 1.5+ 存档（vanilla / MDK / 其他 mod）做 batch 回归；每份用 `analyze_d2s.py` 跑一遍生成 ground truth，再让 GoMule 加载，对比物品数量与 type 序列。
3. 把 `D2BitReader.snapshot` 自动注入 prop 解析失败现场，便于二分定位错位发生的物品和位偏移。

